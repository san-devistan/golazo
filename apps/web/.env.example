# Optional. Set when apps/web uses the Convex backend.
VITE_CONVEX_URL=https://<deployment>.convex.cloud
VITE_CONVEX_SITE_URL=https://<deployment>.convex.site
VITE_SITE_URL=https://golazo.localhost

# Optional storefront fallback conversion. The backend rate action is used when
# Convex is available.
VITE_EUR_TO_USD_RATE=1.10
